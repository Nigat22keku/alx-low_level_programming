#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>

void positive_or_negative(int i);
nt lagest_number(int a, int b, int c);
viod print_remaining_days(int month, int day, int year);
int convert_day(int month, int day);
viod print_times_table(int n);
#endif /* MAIN_H */
